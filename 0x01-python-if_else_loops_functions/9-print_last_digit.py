#!/usr/bin/python3

def print_last_digit(number):
    if number < 0:
        last-dig = (-1 * number) % 10

    else:
        last-dig = number % 10

    print('{}'.format(last-dig), end='')

    return last
